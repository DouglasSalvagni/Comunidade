import AudioPlayer from "@/components/AudioPlayer";

const PlaybackPage = () => {
  return (
    <div>
      {/* O conteúdo principal da página de playback pode ser adicionado aqui */}
      <AudioPlayer />
    </div>
  );
};

export default PlaybackPage;